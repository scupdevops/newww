var expect = require('chai').expect;
var mkhere = require('../lib/mkhere');
var config = require('../config/config.json');

describe('Read configuration', function() {
    it('should read from ./conf/config.json',function(){
        var tplDirName = config.TPL_DIR;
        expect(tplDirName).to.equal('.templates');
    });
});